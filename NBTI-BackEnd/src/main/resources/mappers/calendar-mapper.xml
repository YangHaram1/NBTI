<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="Calendar">
  
  <!-- 캘린더 입력 -->
	<insert id="insert">
		insert into schedule values(schedule_seq.nextval, #{member_id}, #{title}, #{contents}, #{start_date}, #{end_date})
	</insert>
</mapper>

