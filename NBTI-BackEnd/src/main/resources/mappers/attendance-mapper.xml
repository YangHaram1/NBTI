<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="Attendance">
<insert id="insert" parameterType="com.nbti.dto.AttendanceDTO">
    <selectKey keyProperty="seq" resultType="int" order="BEFORE">
        SELECT attendance_seq.NEXTVAL
        FROM dual
    </selectKey>
    INSERT INTO attendance (seq, member_id, today, start_date, end_date)
    VALUES (#{seq}, #{member_id}, SYSDATE, SYSDATE, NULL)
</insert>
<update id="updateClockOut" parameterType="map">
    UPDATE attendance
    SET end_date = #{end_date}
    WHERE seq = #{seq}
</update>
 <select id="selectMemberId" resultType="com.nbti.dto.AttendanceDTO">
 	select * from attendance where member_id = #{member_id}
 </select>
     <select id="getCurrentSeq" parameterType="string" resultType="long">
        SELECT seq
        FROM attendance
        WHERE member_id = #{memberId}
        AND end_date IS NULL
        ORDER BY start_date DESC
        FETCH FIRST 1 ROW ONLY;
    </select>
  </mapper>